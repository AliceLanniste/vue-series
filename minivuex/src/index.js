import { Store,install  } from "./store";
import { mapState,mapMutations,mapActions,mapGetters } from "./helpers";

export default{
    Store,
    install,
    mapState,
    mapMutations,
    mapActions,
    mapGetters,
}


export {
    Store,
    install,
    mapState,
    mapMutations,
    mapActions,
    mapGetters,
}